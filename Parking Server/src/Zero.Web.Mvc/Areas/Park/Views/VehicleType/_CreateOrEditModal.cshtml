@using Zero
@using Zero.Web.Areas.App.Models.Common.Modals
@model Zero.Web.Areas.Park.Models.VehicleType.CreateOrEditVehicleTypeViewModel

@await Html.PartialAsync("~/Areas/App/Views/Common/Modals/_ModalHeader.cshtml", new ModalHeaderViewModel(Model.IsEditMode ? (L("Edit")) : L("Create")))

<div class="modal-body">
    <div id="VehicleTypeInformationsTab">
        <form name="VehicleTypeInformationsForm" role="form" novalidate class="form-validation">

            @if (Model.IsEditMode)
            {
                <input type="hidden" name="id" value="@Model.VehicleType.Id"/>
            }
            <input type="hidden" name="language" id="Entity_Language" value=""/>

            <div class="row">
                <div class="col-12">
                    <div class="form-group">
                        <label for="VehicleType_Name">
                            @L("Name") <span class="text-danger">*</span>
                        </label>
                        <input class="form-control" id="VehicleType_Name" value="@Model.VehicleType.Name" type="text" name="name" required/>
                    </div>
                </div>
            </div>
            
            <div class="row">
                <div class="col-12">
                    <div class="form-group">
                        <label for="VehicleType_Note">
                            @L("Note")
                        </label>
                        <input class="form-control" id="VehicleType_Note" value="@Model.VehicleType.Note" type="text" name="note"/>
                    </div>
                </div>
            </div>
            
        </form>
    </div>
</div>

@await Html.PartialAsync("~/Areas/App/Views/Common/Modals/_ModalFooterWithSaveAndCancel.cshtml")