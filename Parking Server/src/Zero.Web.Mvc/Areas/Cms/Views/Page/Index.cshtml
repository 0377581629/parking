@model Zero.Web.Areas.Cms.Models.Page.PageViewModel
@{
    ViewBag.CurrentPageName = AppPageNames.Cms.Page;
}
@section Scripts
{
    <script abp-src="@ZeroConst.ScriptPathCms/Page/Index.js" asp-append-version="true"></script>
}

<div class="content d-flex flex-column flex-column-fluid" id="kt_content">
    <abp-page-subheader title="@L("Pages")" description="@L("PagesHeaderInfo")">
        @if (IsGranted(CmsPermissions.Page_Create))
        {
            <button id="CreateNewButton" class="btn btn-primary blue">
                <i class="fa fa-plus"></i> @L("Create")
            </button>
        }
    </abp-page-subheader>

    <div class="@(await GetContainerClass())">
        <div class="card card-custom gutter-b">
            <div class="card-body">
                <div class="form">
                    <div id="PagesFormFilter">
                        <div class="row">
                            <div class="col-4">
                                <div class="form-group">
                                    <label>@L("PageTheme")</label>
                                    <select id="PageThemeIdSelector" class="form-control"></select>
                                </div>
                            </div>
                            <div class="col-8">
                                <div class="form-group">
                                    <label>@L("Search")</label>
                                    <div class="input-group">
                                        <input id="PagesTableFilter" class="form-control" type="text" value="@ViewBag.FilterText" placeholder="@L("SearchWithThreeDot")" aria-describedby="basic-addon2">
                                        <span class="input-group-btn">
                                            <button type="button" name="RefreshButton" class="btn btn-primary">
                                                <i class="fa fa-search"></i>
                                            </button>
                                        </span>
                                    </div>
                                </div>
                                
                            </div>
                        </div>
                    </div>
                </div>
                <div class="align-items-center">
                    <table id="PagesTable" class="display table table-striped table-bordered table-hover">
                        <thead>
                        <tr>
                            <th></th>
                            <th class="text-center">@L("Actions")</th>
                            <th class="text-center">@L("Title")</th>
                            <th class="text-center">@L("About")</th>
                            <th class="text-center">@L("Slug")</th>
                            <th class="text-center">@L("PageTheme")</th>
                            <th class="text-center">@L("PageLayout")</th>
                            <th class="text-center">@L("IsHomePage")</th>
                            <th class="text-center">@L("Publish")</th>
                            <th class="text-center">@L("IsActive")</th>
                        </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>